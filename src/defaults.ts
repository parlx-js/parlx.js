import { Settings } from './types';

export const defaultSettings: Settings = {
  axis: 'Y',
  base: window,
  direction: 'vertical',
  exclude: null,
  height: '400px',
  speed: 0.3,
  type: 'background'
};
